name: ci-wf   
on:     
  push:
   branches:
     - develop
  pull_request:
    branches:
     - develop
jobs:
 linting-job:
   runs-on: ubuntu-latest
   steps:
    - name: Welcome Meassge
      run: echo "Welcome to CI Pipeline"
    - name: Cloning Repository
      uses: actions/checkout@v4
